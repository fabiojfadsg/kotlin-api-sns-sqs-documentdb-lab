# Infraestrutura local

MongoDB e emulação SNS/SQS via Compose. Consulte [execução e limites](../backend/README.md).
O script init-localstack.sh cria tópico, filas, política e assinatura de forma idempotente.
Nenhum recurso AWS real é provisionado.
